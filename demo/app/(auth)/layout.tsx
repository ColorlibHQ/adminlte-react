export const metadata = {
  title: { default: 'AdminLTE React', template: '%s · AdminLTE React' },
}

export default function AuthGroupLayout({ children }: { children: React.ReactNode }) {
  return children
}
