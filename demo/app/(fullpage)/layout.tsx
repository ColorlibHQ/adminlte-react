export const metadata = {
  title: { default: 'Layout', template: '%s · AdminLTE React' },
}

export default function FullpageGroupLayout({ children }: { children: React.ReactNode }) {
  return children
}
